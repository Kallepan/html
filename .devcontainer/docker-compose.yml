services:
  devcontainer:
    build: .
    volumes:
      - ../.:/workspace:cached
      - ../src:/var/www/html
      - ../nginx.conf:/etc/nginx/nginx.conf:ro
      - ~/.ssh:/root/.ssh
    ports:
      - 80:80